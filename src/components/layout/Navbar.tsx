'use client';

import Link from 'next/link';

const Navbar= () => {
    return (
        <nav>
            {
                /* Navbar content goes here */
            }
        </nav>
    );
};

export default Navbar;
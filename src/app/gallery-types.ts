// Re-export gallery types for API use
export type { GalleryImage, GalleryCategory, GallerySortBy } from "../(admin)/admin/(siteSettngs)/gallery/types";
export { GALLERY_CATEGORIES, SORT_OPTIONS, CATEGORY_COLORS, CATEGORY_ICONS, MOCK_GALLERY_IMAGES } from "../(admin)/admin/(siteSettngs)/gallery/types";
